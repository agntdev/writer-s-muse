[
  {
    "name": "Get idea",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Generate writing prompts based on genre/mood"
            }
          }
        ],
        "send": {
          "callback": "idea:generate"
        }
      }
    ]
  }
]
